<?xml version="1.0" encoding="UTF-8"?>
<cronentries>
  <!--  cron>
    <url>/fetchMarketOverview?url=http://api.intrade.com/jsp/XML/MarketData/xml.jsp</url>
    <description>Fetch the active and expired Intrade contracts every few hours. Get the cached copy because calling directly Intrade times out Google App Engine</description>
    <schedule>every 1 month</schedule>
  </cron>
  <cron>
    <url>/storeClosingPrices</url>
    <description>Fetch the daily closing prices</description>
    <schedule>every 30 days</schedule>
  </cron>
    <cron>
    <url>/storeTrades</url>
    <description>Fetch the trades for all active contracts</description>
    <schedule>every 1 month</schedule>
  </cron>
   -->
</cronentries>